{
  "name": "language-learner",
  "version": "1.0.0",
  "description": "Japanese language learning roadmap app",
  "scripts": {
    "build": "cd client && npm ci && npm run build",
    "install:all": "cd client && npm ci && cd ../server && npm ci",
    "start": "node server/index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
